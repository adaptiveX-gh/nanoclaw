[project]
name = "kata-mcp"
version = "0.1.0"
description = "MCP server for kata strategy improvement — benchmarking, market inspection, knowledge, and discipline enforcement."
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.5.0",
    "pandas>=2.0",
    "TA-Lib>=0.4.28",
]
